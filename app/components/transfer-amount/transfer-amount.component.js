//Amount Transfer Component

angular.module('transferAmount').component('transferAmount', {
        restrict: 'E',
        transclude: true,
        templateUrl: './components/transfer-amount/transfer-amount.template.html',
})